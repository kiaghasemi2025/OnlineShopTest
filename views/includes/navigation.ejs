    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link <%= path === '/' ? "active" : ""  %>" href="/">خانه</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= path === '/products' ? "active" : ""  %>" href="/products">محصولات</a>
          </li>
          <% if(isAuthenticated) { %>
          <li class="nav-item">
            <a class="nav-link <%= path == '/cart' ? "active" : ""  %>  " href="/cart">سبدخرید</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= path === '/orders' ? "active" : ""  %>  " href="/orders">سفارشات</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= path === '/admin/add-product' ? "active" : "" %>" href="/admin/add-product">افزودن محصول</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= path === '/admin/products' ? "active" : ""  %>" href="/admin/products">محصولات ادمین</a>
          </li>
          <form method="post" action="/logout">
              <button id="navbtn" type="submit">خروج</button>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          </form>
          <% }  %>
          <% if (!isAuthenticated) { %>
          <li class="nav-item">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <a class="nav-link" href="/login">ورود</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/signup">ثبت نام</a>
          </li>
          <% } %>
        </ul>
      </div>
    </nav>